//configuration options

module.exports = {
  secret = "poorsecret", //used for authentication.
                     //Should come up with a more secure handler for this...

  db = 'localhost:27018/cpsc310', //path to database
  port = '3000',     //port to serve on
};
