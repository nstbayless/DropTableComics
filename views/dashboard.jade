extends pretty

block content
	h1.bannertext= header
	ul
		each comic, i in editable
			li
				a(href="/pretty/see/" + comic.creator + "/" + comic.title)= comic.title 
				br
				a(href="/pretty/" + comic.creator + "/" + "adminpage" + "/" + comic.title)= "Edit Permissions" 